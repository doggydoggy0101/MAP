from setuptools import setup

setup(
    name='map_python',
    version='0.1.0',
    author='Bang-Shien Chen',
    description='Python binding for MAP',
    package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}'},
    packages=['map_python'],
    package_data={'': ['*.so']}
)